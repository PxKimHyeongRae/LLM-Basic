# 🌡️ IoT 센서 데이터 기반 전광판 문구 생성 시스템

KORMo-10B-sft AI 모델을 사용하여 IoT 센서 데이터를 분석하고, 시민들을 위한 친근한 한국어 전광판 문구를 자동으로 생성하는 시스템입니다.

## ✨ 주요 기능

- ✅ MariaDB에서 IoT 온도/습도 센서 데이터 자동 조회
- ✅ 어제와 오늘의 평균 온도 비교
- ✅ 일교차, 평균 온도/습도 등 자동 분석
- ✅ KORMo-10B AI 모델로 자연스러운 한국어 문구 생성
- ✅ 빠른 응답 속도 (모델 서버 방식: 약 5초)
- ✅ 생성 기록 자동 로깅

## 📋 실행 결과 예시

```
======================================================================
[전광판 출력] 어제보다 약간 쌀쌀해졌으니, 아침엔 두꺼운 옷 꼭 챙기세요
======================================================================

✓ 모든 작업 완료
총 실행 시간: 5.46초
```

---

## 🚀 빠른 시작 (3단계)

### 1단계: 설치

```bash
# 1. 저장소 클론 또는 파일 다운로드
cd /your/project/path

# 2. 필요한 패키지 설치
pip install -r requirements.txt
```

**필수 패키지:**
- transformers, torch, accelerate, bitsandbytes (AI 모델용)
- fastapi, uvicorn (모델 서버용)
- pymysql (데이터베이스 연결)
- requests (API 통신)

### 2단계: 환경 설정

`.env` 파일을 열어 데이터베이스 정보를 입력하세요:

```bash
# 데이터베이스 연결 정보
DB_HOST=192.168.4.8
DB_PORT=3307
DB_NAME=a_iot_old
DB_USER=your_username
DB_PASSWORD=your_password

# LLM 모델 설정
MODEL_PATH=KORMo-Team/KORMo-10B-sft
QUANTIZATION=8bit  # 4bit (6GB RAM) 또는 8bit (10GB RAM)

# 모델 서버 설정 (기본값 사용 권장)
USE_MODEL_SERVER=true
MODEL_SERVER_URL=http://localhost:8000
MODEL_SERVER_PORT=8000
```

### 3단계: 실행

**터미널 1** - 모델 서버 시작 (최초 1회, 계속 켜두기):
```bash
python model_server.py
```

첫 실행 시 KORMo 모델이 자동으로 다운로드됩니다 (약 20GB, 10분 소요).
이후 실행부터는 캐시된 모델을 사용합니다.

서버가 준비되면 다음 메시지가 표시됩니다:
```
✓ 모델 로드 완료! (소요 시간: 135.2초)
서버 준비 완료! API 요청을 받을 수 있습니다.
```

**터미널 2** - 메인 프로그램 실행:
```bash
python main.py
```

**완료!** 이제 약 5초 만에 전광판 문구가 생성됩니다.

---

## 📖 상세 사용법

### 기본 실행

```bash
# 기본 실행 (2025년 4월 10일 기준, 어제와 오늘 비교)
python main.py

# 특정 날짜 지정
python main.py --date 2025-04-15

# 어제와 비교하지 않기
python main.py --no-compare

# 여러 개의 문구 생성 (3개)
python main.py --multiple

# 특정 센서만 사용
python main.py --devices SNIOT-P-THM-008 SNIOT-P-TST-057
```

### 실행 모드

시스템은 두 가지 모드로 작동합니다:

#### 1. API 모드 (기본값, 권장) ⚡️
```bash
# .env 파일
USE_MODEL_SERVER=true
```
- **장점**: 매우 빠름 (약 5초)
- **방법**: 모델 서버를 한 번만 실행하고 계속 사용
- **필요**: 터미널 2개 (서버용 1개 + 실행용 1개)

#### 2. 직접 로드 모드 🐌
```bash
# .env 파일
USE_MODEL_SERVER=false
```
- **장점**: 서버 실행 불필요
- **단점**: 매번 모델 로딩 (약 2~3분 소요)
- **사용 시기**: 테스트 또는 단발성 실행

---

## 🔧 모델 서버 관리

### 서버 상태 확인

브라우저에서 접속:
```
http://localhost:8000
```

또는 터미널에서:
```bash
# 헬스 체크
curl http://localhost:8000/health

# 응답 예시
{
  "status": "healthy",
  "model_loaded": true,
  "cuda_available": true
}
```

### 서버 재시작

```bash
# 터미널 1에서 Ctrl+C로 종료 후
python model_server.py
```

### 서버 없이 재로드 (설정 변경 시)

```bash
# .env 파일 수정 후
curl -X POST http://localhost:8000/reload
```

---

## 🌐 API 직접 사용

모델 서버는 HTTP API로 직접 사용할 수 있습니다.

### Python으로 API 호출

```python
import requests

# 텍스트 생성 요청
response = requests.post(
    "http://localhost:8000/generate",
    json={
        "prompt": "오늘은 따뜻한 날씨입니다. 시민들에게 한 문장으로 알려주세요:",
        "max_new_tokens": 50,
        "temperature": 0.5,
        "top_p": 0.85,
        "repetition_penalty": 1.2
    }
)

result = response.json()
print(f"생성된 문구: {result['generated_text']}")
print(f"소요 시간: {result['generation_time']:.2f}초")
```

### curl로 API 호출

```bash
curl -X POST http://localhost:8000/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "오늘 일교차가 큽니다. 시민들에게 조언:",
    "max_new_tokens": 50,
    "temperature": 0.5
  }'
```

### API 엔드포인트

| 엔드포인트 | 메서드 | 설명 |
|----------|--------|------|
| `/` | GET | 서버 정보 조회 |
| `/health` | GET | 서버 상태 확인 |
| `/generate` | POST | 텍스트 생성 |
| `/reload` | POST | 모델 재로드 |

### 요청 파라미터

```json
{
  "prompt": "입력 프롬프트 (필수)",
  "max_new_tokens": 50,        // 생성할 최대 토큰 수
  "temperature": 0.5,           // 0.0~1.0, 낮을수록 일관됨
  "top_p": 0.85,                // Nucleus sampling
  "repetition_penalty": 1.2,    // 반복 방지
  "do_sample": true             // 샘플링 사용 여부
}
```

### 응답 형식

```json
{
  "generated_text": "생성된 텍스트",
  "prompt": "입력한 프롬프트",
  "generation_time": 5.37
}
```

---

## 📁 프로젝트 구조

```
llm/
├── main.py                    # 메인 실행 파일
├── model_server.py            # 모델 서버
├── requirements.txt           # 필요한 패키지
├── .env                       # 환경 설정 (중요!)
│
├── src/
│   ├── database/
│   │   └── connection.py      # 데이터베이스 연결
│   │
│   ├── analyzer/
│   │   ├── sensor_data_fetcher.py  # 센서 데이터 조회
│   │   └── weather_analyzer.py     # 날씨 분석
│   │
│   ├── generator/
│   │   ├── llm_generator_local_api.py  # API 클라이언트
│   │   └── prompt_templates.py         # 프롬프트 템플릿
│   │
│   └── output/
│       └── display_handler.py  # 출력 및 로깅
│
└── data/
    ├── logs/
    │   ├── system.log          # 시스템 로그
    │   └── display_messages_*.jsonl  # 생성 기록
    └── current_message.txt     # 현재 전광판 문구
```

---

## ⚙️ 시스템 요구사항

### 최소 사양
- **OS**: Linux, Windows, macOS
- **Python**: 3.8 이상
- **RAM**: 16GB 이상
- **GPU**: NVIDIA GPU (CUDA 지원)
  - 4bit 양자화: 6GB VRAM
  - 8bit 양자화: 10GB VRAM (권장)
- **디스크**: 약 30GB (모델 20GB + 기타 10GB)
- **인터넷**: 첫 실행 시 모델 다운로드용

### 권장 사양
- **RAM**: 32GB
- **GPU**: RTX 3090 (24GB) 이상
- **디스크**: SSD

---

## 🔍 성능 비교

### 실행 시간

| 모드 | 첫 실행 | 이후 실행 | 총 시간 (3회) |
|------|---------|-----------|--------------|
| **직접 로드** | 235초 | 235초 | **705초** |
| **API 모드** | 135초 (서버 시작) + 5초 | 5초 | **145초** |

➡️ **API 모드가 약 5배 빠릅니다!**

### 양자화 비교

| 양자화 | GPU 메모리 | 로딩 시간 | 생성 속도 | 품질 |
|--------|-----------|----------|----------|------|
| **4bit** | ~6GB | ~2분 | 빠름 | 좋음 |
| **8bit** | ~10GB | ~2.5분 | 보통 | **더 좋음** ⭐ |
| none | ~20GB | ~3분 | 느림 | 최고 |

**권장**: 8bit (품질과 성능의 균형)

---

## 🛠️ 문제 해결

### 1. 서버 연결 실패
```
ERROR: 서버 연결 실패. model_server.py가 실행 중인지 확인하세요.
```

**해결**: 터미널 1에서 `python model_server.py` 실행

### 2. GPU 메모리 부족
```
CUDA out of memory
```

**해결**: `.env` 파일에서 양자화 변경
```bash
QUANTIZATION=4bit  # 8bit → 4bit로 변경
```

### 3. 모델 다운로드 느림

첫 실행 시 20GB 모델 다운로드 (10~30분 소요)

**해결**:
```bash
# 다운로드 진행 상황 확인
# HuggingFace 캐시 위치: ~/.cache/huggingface/hub/
du -sh ~/.cache/huggingface/
```

### 4. 포트 충돌
```
Address already in use
```

**해결**: `.env`에서 다른 포트 사용
```bash
MODEL_SERVER_PORT=8001
```

### 5. 데이터베이스 연결 실패
```
ERROR: 데이터베이스 연결 실패
```

**해결**:
1. `.env` 파일의 DB 정보 확인
2. 데이터베이스 서버 실행 확인
3. 방화벽 설정 확인

---

## 📊 출력 로그 확인

### 시스템 로그
```bash
cat data/logs/system.log
```

### 생성 기록
```bash
# 최근 생성된 문구들 확인
cat data/logs/display_messages_202504.jsonl | jq .
```

### 현재 전광판 문구
```bash
cat data/current_message.txt
```

---

## 🎨 커스터마이징

### 1. 프롬프트 수정

`src/generator/prompt_templates.py` 파일 수정:

```python
def get_temperature_comparison_prompt(comparison_data: dict) -> str:
    # 여기서 프롬프트 템플릿 수정
    prompt = f"""당신만의 프롬프트 작성..."""
    return prompt
```

### 2. 분석 지표 추가

`src/analyzer/weather_analyzer.py` 파일에서 새로운 분석 로직 추가

### 3. 전광판 연동

`src/output/display_handler.py`의 `send_to_display()` 메서드를 실제 전광판 시스템과 연동

---

## 💡 유용한 팁

### 1. 백그라운드 실행 (Linux)

```bash
# 모델 서버를 백그라운드로 실행
nohup python model_server.py > model_server.log 2>&1 &

# 프로세스 확인
ps aux | grep model_server

# 종료
pkill -f model_server.py
```

### 2. 자동 재시작 (systemd)

`/etc/systemd/system/kormo-model.service`:
```ini
[Unit]
Description=KORMo Model Server
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/llm
ExecStart=/path/to/python model_server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable kormo-model
sudo systemctl start kormo-model
```

### 3. 크론 스케줄링

```bash
# 매일 오전 8시에 문구 생성
crontab -e

# 추가
0 8 * * * cd /path/to/llm && /path/to/python main.py >> data/logs/cron.log 2>&1
```

---

## 📚 추가 정보

### 사용 모델
- **KORMo-10B-sft**: 한국어 특화 LLM (10B 파라미터)
- **출처**: [HuggingFace - KORMo-Team](https://huggingface.co/KORMo-Team/KORMo-10B-sft)
- **라이선스**: 확인 필요 (비상업용 무료 사용 가능)

### 기술 스택
- **AI Framework**: HuggingFace Transformers, PyTorch
- **Web Framework**: FastAPI, Uvicorn
- **Database**: PyMySQL, SQLAlchemy
- **Quantization**: bitsandbytes

### 참고 자료
- [HuggingFace Transformers](https://huggingface.co/docs/transformers)
- [FastAPI 문서](https://fastapi.tiangolo.com/)
- [bitsandbytes 문서](https://github.com/TimDettmers/bitsandbytes)

---

## 🤝 기여

이슈 및 개선 사항은 GitHub Issues에 등록해주세요.

---

## 📄 라이선스

- **프로젝트 코드**: MIT License
- **KORMo 모델**: 모델 라이선스 참조

---

## 📞 지원

문제가 발생하면:
1. `data/logs/system.log` 확인
2. GitHub Issues 검색
3. `.env` 설정 재확인
4. 모델 서버 재시작

---

**🎉 축하합니다! 이제 AI 기반 전광판 문구 생성 시스템을 사용할 준비가 되었습니다!**
